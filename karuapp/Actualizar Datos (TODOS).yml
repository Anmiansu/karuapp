info:
  name: Actualizar Datos (TODOS)
  type: http

http:
  method: PUT
  url: http://localhost:3000/api/platos/32
  body:
    type: json
    data: |-
      {
        "nombre": "milanesa",
        "precio": 45000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
